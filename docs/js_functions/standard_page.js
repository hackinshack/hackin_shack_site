class Standard_Page {

    constructor(n_sections) {
        this.sections = n_sections;
        this.create_sections();
    }

    create_sections() {
        for (var i=0; i<this.sections; i++) {
            // add an embedded sketch and aside section
            
        }
    }
}